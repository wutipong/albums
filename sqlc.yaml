version: "2"
servers:
- engine: postgresql
  uri: ${DATABASE_URL}
sql:
  - engine: "postgresql"
    queries: 
      - "db/queries/albums.sql"
      - "db/queries/assets.sql"
    schema: "db/migrations/"
    database:
      managed: true