version: "2"
servers:
- engine: postgresql
  uri: ${DATABASE_URL}
sql:
  - engine: "postgresql"
    queries: 
      - "../db/queries/assets.sql"
    schema: "../db/migrations/"
    database:
      managed: true
    gen:
      go:
        package: "db"
        out: "db"
        sql_package: "pgx/v5"